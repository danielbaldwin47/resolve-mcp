# mcp-tests-zinc — agent-owned project root

Zinc Bar, 2026-06-17, Set 2. Ryan Devlin's band. Resolve project `mcp-tests-zinc`,
sync-reference timeline `Zinc SYNC` (74 min, 23.976 fps), original human cut timeline
`Zinc - Set 2 Main`.

- `songs.json` — song key → title + personnel, per `docs/agents/rough-cut.md`
  §songs.json. Titles are taken verbatim from the director's delivered filenames
  (`6-17 - Zinc Set 2 - <title>.mp4`). **Personnel is unverified**: only the leader
  (Ryan Devlin) is known, and his instrument is not; every other chair is marked
  `unknown` and is a question for the director before any title is rendered.
- Song keys are slugs of the titles. `Zinc SYNC` currently carries **zero markers**, so
  no blue marker exists for any key yet — the keys are proposals, not confirmations
  (the concert pillar has the director confirm blue markers before `songs.json` is
  authoritative).

Where each song sits in the master mix — measured, not guessed — is
`gauntlet/recon/songs_map.json`.

Master mix: `P:\Client Work\Ryan Devlin\2026-06-17_Zinc Bar\Audio\Reaper\Zinc Set 2 Reaper v4.wav`,
sitting on `Zinc SYNC` at record frame 86401 (its own frame 0).
